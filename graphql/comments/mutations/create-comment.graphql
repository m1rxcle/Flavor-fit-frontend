mutation CreateComment($input: CreateCommentInput!) {
	createComment(input: $input) {
		id
		authorId
		author {
			id
			profile {
				fullName
				imageUrl
			}
		}
		recipeId
		content
		createdAt
		updatedAt
	}
}
