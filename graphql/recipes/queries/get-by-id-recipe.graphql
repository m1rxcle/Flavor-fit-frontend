query GetByIdRecipe($id: String!) {
	getRecipeById(id: $id) {
		id
		title
		description
		difficulty
		nutritionFact {
			carbohydrates
			fats
			fiber
		}
		recipeIngredients {
			id
			quantity
			unit
		}
	}
}
