query SearchRecipe($searchTerm: String!) {
	searchRecipes(searchTerm: $searchTerm) {
		id
		imageUrl
		title
		description
		calories
		cookingTime
		difficulty
		slug

		likes {
			userId
		}

		views {
			id
			userId
		}
	}
}
