query GetLikedRecipes($take: Float!) {
	getLikedRecipes(take: $take) {
		id
		title
		slug
		description
		imageUrl
		calories
		cookingTime
		difficulty
		likes {
			id
			userId
		}

		views {
			id
			userId
		}
	}
}
